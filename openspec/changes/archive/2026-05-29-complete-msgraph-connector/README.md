# complete-msgraph-connector

Bring the Microsoft Graph calendar connector up to functional parity with the Google connector: real field mapping in both directions, paginated event access, and a complete `CalendarCollection` contract (merge / export / removeAll / collection creation overloads).
